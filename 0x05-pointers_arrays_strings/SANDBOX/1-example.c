#include <stdio.h>

/**
 * main - addresses of variables
 *
 * Return: Always 0.
 */
int main(void)
{
   char captain;
   int crunch;

   printf("Address of variable 'captain': %p\n", &captain);
   printf("Address of variable 'crunch': %p\n", &crunch);
   return (0);
}
