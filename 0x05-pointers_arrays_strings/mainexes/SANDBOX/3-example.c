#include <stdio.h>

/**
 * main - printing the address of a pointer
 *
 * Return: Always 0.
 */

int main(void)
{
	int *pointer;

	printf("Address of variable 'pointer': %p\n", &pointer);
	return (0);
}
