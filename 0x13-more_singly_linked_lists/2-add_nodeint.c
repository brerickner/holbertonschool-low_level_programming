#include "lists.h"
#include <stdio.h>

/**
 * listint_t *add_nodeint - listint_t **head, const int n
 * @h: pointer to node in listint_h
 * definition: function that adds a new node at beginning of listint_t
 * Return: address of new element. Else NULL if failed.
 */
listint_t *add_nodeint(listint_t **head, const int n)
{
	(void)head;
	(void)n;

	return(0);
}
