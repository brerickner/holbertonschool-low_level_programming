#include <stdio.h>
int main(void)
{
  int x = '0';
  int y = '0';

  if (x = 0 && y <= 9)
    {
      putchar(x);
      putchar(y);
      putchar(',');
      putchar(' ');
      putchar('\n');
      y++;
    }

  else
    x++;
  return (0);
}
